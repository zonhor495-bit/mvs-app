# 🏗️ ТЕХНИЧЕСКАЯ АРХИТЕКТУРА - НОВАЯ СИСТЕМА АУТЕНТИФИКАЦИИ\n\n## Общая Схема\n\n```\nApp.tsx (Entry Point)\n  ↓\nInternalAppWithAuth (3-state machine)\n  ├─ 'init-setup'  → InitSetup\n  ├─ 'role-login'  → RoleLogin\n  └─ 'app'         → InternalApp (с onLogout)\n       ↓\n    Layout (меню ролей)\n       ↓\n    Остальное UI\n```\n\n## Компоненты и Ответственность\n\n### 1. InitSetup.tsx\n**Ответственность**: Первоначальная настройка паролей\n\n```typescript\ninterface InitSetupProps {\n  onSetupComplete: (adminPassword: string, managerPassword: string) => void;\n}\n\n// Валидация\n- Минимум 4 символа\n- Пароли должны отличаться\n- Подтверждение должно совпадать\n\n// Сохранение\nlocalStorage['wd_auth_passwords'] = {admin, manager}\nlocalStorage['wd_is_initialized'] = true\n```\n\n### 2. RoleLogin.tsx\n**Ответственность**: Двухступенчатая аутентификация\n\n```typescript\ninterface RoleLoginProps {\n  onLogin: (role: 'admin' | 'manager', password: string) => void;\n  isLoading?: boolean;\n}\n\n// Flow\nStep 1: selectRole\n  - Выбрать админ или менеджер\nStep 2: enterPassword\n  - Ввести пароль для выбранной роли\n  - verifyPassword() проверяет\n  - onLogin() вызывается при успехе\n```\n\n### 3. InitSetup vs RoleLogin\n\n| Компонент | Когда | Задача | Пароли Новые? |\n|-----------|-------|--------|---------------|\n| InitSetup | Первый запуск | Создать пароли | Да (создание) |\n| RoleLogin | Каждый запуск | Ввести пароль | Нет (проверка) |\n\n### 4. Layout.tsx - Меню Ролей\n**Ответственность**: Переключение ролей во время сеанса\n\n```typescript\n// Кнопка в правом верхнем углу\nbutton: {roleIcon} {roleLabel}\n\n// Меню содержит\n1. Администратор (с иконкой 👤)\n2. Управляющий (с иконкой 👑)\n3. Выйти из аккаунта (с иконкой 🚪)\n\n// При переключении\nif (role !== currentRole) {\n  → Модальное окно с запросом пароля\n  → verifyPassword() проверяет\n  → onRoleChange() срабатывает\n}\n```\n\n## Store Functions\n\n### Ключевые Функции\n\n```typescript\n// Инициализация\nisAppInitialized(): boolean                    // Флаг первого запуска\nsetAppInitialized(): void                      // Установить флаг\n\n// Управление паролями\nsaveAuthPasswords({admin, manager}): void      // Сохранить пароли\ngetAuthPasswords(): {admin, manager} | null    // Получить пароли\nverifyPassword(role, password): boolean        // Проверить пароль\n\n// Управление ролью\nsetCurrentRole(role): void                     // Установить текущую роль\ngetCurrentRole(): 'admin' | 'manager'          // Получить текущую роль\n\n// Сеанс\nlogout(): void                                 // Выход (очистить роль и сеанс)\n```\n\n## State Machine в App.tsx\n\n```typescript\ntype AppState = 'init-setup' | 'role-login' | 'app';\n\nconst [appState, setAppState] = useState<AppState>('init-setup');\n\nuseEffect(() => {\n  const isInit = isAppInitialized();\n  setAppState(isInit ? 'role-login' : 'init-setup');\n}, []);\n\n// Переходы\ninit-setup\n  ↓ onSetupComplete()\n  ↓ handleInitSetupComplete()\n  ↓ saveAuthPasswords()\n  ↓ handleRoleLogin('admin', password)\napp  (с FirstRunSetup)\n  ↓ logout\nrole-login\n\nrole-login\n  ↓ onLogin(role, password)\n  ↓ handleRoleLogin(role, password)\n  ↓ verifyPassword() ✓\n  ↓ setCurrentRole()\napp\n  ↓ onLogout() (Layout меню)\nrole-login\n```\n\n## Permission System\n\n### Enum Permission\n\n```typescript\nenum Permission {\n  VIEW_ORDERS = 'view_orders',\n  MANAGE_ORDERS = 'manage_orders',\n  VIEW_CLIENTS = 'view_clients',\n  // ... 20+ разрешений\n}\n```\n\n### Role Permissions Mapping\n\n```typescript\nconst ROLE_PERMISSIONS: Record<UserRole, RolePermissions> = {\n  admin: {\n    permissions: [\n      Permission.VIEW_ORDERS,\n      Permission.MANAGE_ORDERS,\n      Permission.VIEW_CLIENTS,\n      Permission.VIEW_PRICING,\n      Permission.PROCESS_PAYMENT,\n      Permission.VIEW_CASHIER,\n      Permission.VIEW_ANALYTICS,\n      Permission.VIEW_REPORTS,\n    ],\n  },\n  manager: {\n    permissions: [\n      // ВСЕ разрешения (~35+)\n    ],\n  },\n};\n```\n\n### Utility Functions\n\n```typescript\nhasPermission(role: UserRole, permission: Permission): boolean\n\ncanViewPage(role: UserRole, pagePermissions: Permission[]): boolean\n\ngetRolePermissions(role: UserRole): Permission[]\n```\n\n## Data Flow: Первый Запуск\n\n```\n1. App Loads\n   ↓\n2. InternalAppWithAuth checks isAppInitialized()\n   ↓ (returns false)\n   ↓\n3. Show InitSetup\n   ↓\n4. User enters passwords\n   ↓\n5. handleInitSetupComplete(adminPass, managerPass)\n   ↓\n6. saveAuthPasswords({admin: adminPass, manager: managerPass})\n   ↓\n7. handleRoleLogin('admin', adminPass)\n   ↓\n8. verifyPassword('admin', adminPass) → true\n   ↓\n9. setCurrentRole('admin')\n   ↓\n10. setAppState('app')\n    ↓\n11. InternalApp renders\n    ↓ (!user)\n    ↓ !activeOrg\n12. FirstRunSetup shows\n    ↓\n13. User creates first car wash\n    ↓\n14. Layout renders\n```\n\n## Data Flow: Переключение Ролей\n\n```\n1. User clicks role button (👤)\n   ↓\n2. Menu opens with options\n   ↓\n3. User clicks different role\n   ↓\n4. handleRoleSelect(newRole)\n   ↓\n5. setRolePromptOpen(true)\n   ↓\n6. Modal with password field shows\n   ↓\n7. User enters password\n   ↓\n8. confirmRoleSwitch()\n   ↓\n9. verifyPassword(newRole, password) → true/false\n   ↓ (true)\n   ↓\n10. onRoleChange(newRole)\n    ↓\n11. InternalApp.handleRoleChange(newRole)\n    ↓\n12. updateUserProfile(userId, {role: newRole})\n    ↓\n13. setUser(updated)\n    ↓\n14. UI re-renders with new permissions\n```\n\n## localStorage Structure\n\n```javascript\n{\n  // Инициализация\n  wd_is_initialized: \"true\",\n  \n  // Пароли (⚠️ видны в DevTools!)\n  wd_auth_passwords: JSON.stringify({\n    admin: \"admin123\",\n    manager: \"manager456\"\n  }),\n  \n  // Текущая роль\n  wd_current_role: \"admin\",\n  \n  // Сеанс пользователя (от store)\n  wd_session: JSON.stringify({\n    userId: \"user-123\",\n    activeOrgId: \"org-456\"\n  }),\n  \n  // Остальные данные приложения...\n}\n```\n\n## Безопасность: Текущее Состояние\n\n### ⚠️ Не Безопасно для Production\n\n```typescript\n// ❌ Пароли в localStorage в открытом виде\nlocalStorage['wd_auth_passwords'] = {admin, manager};\n\n// ❌ Проверка пароля на клиенте\nverifyPassword(role, password): boolean {\n  const stored = getAuthPasswords();\n  return stored[role] === password;  // Сравнение на клиенте!\n}\n```\n\n### ✅ Рекомендации для Production\n\n```typescript\n// 1. Хеширование паролей\nimport bcrypt from 'bcrypt';\nconst hashed = await bcrypt.hash(password, 10);\nlocalStorage['wd_auth_passwords'] = {admin: hashed, manager: hashed};\n\n// 2. Backend Verification\nconst response = await fetch('/api/auth/verify-password', {\n  method: 'POST',\n  body: JSON.stringify({role, password}),\n  credentials: 'include'  // HTTPS + Cookies\n});\nconst {valid} = await response.json();\n\n// 3. JWT Tokens\nconst token = response.headers['authorization'];\nlocalStorage['auth_token'] = token;\n\n// 4. Secure Headers\nresponse.headers['Set-Cookie'] = 'token=...; Secure; HttpOnly';\n```\n\n## Обработка Ошибок\n\n```typescript\n// InitSetup.tsx\nif (adminPassword !== adminConfirm) {\n  setError('Пароли не совпадают');\n  return;\n}\nif (adminPassword === managerPassword) {\n  setError('Пароли должны отличаться');\n  return;\n}\n\n// RoleLogin.tsx\nif (!verifyPassword(role, password)) {\n  setError('Неверный пароль');\n  return;\n}\n\n// Layout.tsx (переключение ролей)\nif (!verifyPassword(role, password)) {\n  setRoleError('Неверный пароль');\n  return;\n}\n```\n\n## Performance Considerations\n\n### Оптимизации\n\n```typescript\n// 1. Lazy Loading компонентов\nconst RoleLogin = lazy(() => import('./RoleLogin'));\nconst InitSetup = lazy(() => import('./InitSetup'));\n\n// 2. Memoization функций\nconst handleRoleLogin = useCallback((role, password) => {\n  // ...\n}, []);\n\n// 3. Conditional Rendering\nif (appState === 'init-setup') return <InitSetup />;\nif (appState === 'role-login') return <RoleLogin />;\nreturn <InternalApp />;\n\n// 4. Suspense для async операций\n<Suspense fallback={<Loading />}>\n  {children}\n</Suspense>\n```\n\n## Типизация\n\n```typescript\n// types.ts\ntype UserRole = 'admin' | 'manager';\ntype AppState = 'init-setup' | 'role-login' | 'app';\n\ninterface InitSetupProps {\n  onSetupComplete: (adminPassword: string, managerPassword: string) => void;\n}\n\ninterface RoleLoginProps {\n  onLogin: (role: 'admin' | 'manager', password: string) => void;\n  isLoading?: boolean;\n}\n\ninterface AuthPasswords {\n  admin: string;\n  manager: string;\n}\n\ninterface RolePermissions {\n  permissions: Permission[];\n}\n```\n\n## Миграция со Старой Системы\n\n### Что Было\n```typescript\nconst isAdmin = user.role === 'admin';\nif (isAdmin) {\n  // show admin features\n}\n```\n\n### Что Стало\n```typescript\nconst canViewPricing = hasPermission(user.role, Permission.VIEW_PRICING);\nif (canViewPricing) {\n  // show pricing features\n}\n```\n\n### Преимущества\n- ✅ Легче добавлять разрешения\n- ✅ Легче добавлять новые роли\n- ✅ Более явное выражение требований\n- ✅ Лучше для тестирования\n\n## Следующие Шаги\n\n1. **Тестирование** - Запустить AUTH_SYSTEM_TESTING.md сценарии\n2. **Замена проверок** - Заменить `user.role === 'admin'` на `hasPermission()`\n3. **Backend** - Перенести валидацию паролей на backend\n4. **Логирование** - Добавить логирование входов для аудита\n5. **MFA** - Добавить двухфакторную аутентификацию\n\n---\n\n**Автор**: AI Assistant\n**Дата**: 2024\n**Версия**: 1.0\n**Статус**: ✅ ПОЛНОСТЬЮ ДОКУМЕНТИРОВАНО\n"